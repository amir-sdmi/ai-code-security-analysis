// Code generated by CoPilot 
//get GetAuthenticate1 response and request params
var usernameVar = context.getVariable("request.formparam.username"),
    passwordVar = context.getVariable("request.formparam.password");

//fill-in username and password and setup GraphQL mutation
var query = "mutation getConsumerToken($input: ConsumerTokenInput!) { getConsumerToken(input: $input) { success { access_token token_type expires_in } error { message } } }";

var variables = {
        "input": {
            "username": usernameVar,
            "password": passwordVar
        }
};

//set updated request
context.setVariable('payloadQuery', query);
context.setVariable('payloadVariable', JSON.stringify(variables));