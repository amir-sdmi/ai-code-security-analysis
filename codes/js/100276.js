import { Inter } from "next/font/google";
import "./globals.css";
import Image from 'next/image'; // import Image from next.js
import backgroundImage from '@/app/public/backgroundImage.jpg'; // import the image

const inter = Inter({ subsets: ["latin"] });

export const metadata = {
  title: "GROK AI",
  description: "Recipe generated by Grok AI",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={`${inter.className}`}>
        {/* <div style={{ 
          position: 'absolute', 
          top: 0, 
          left: 0, 
          width: '100%', 
          height: '100%', 
          zIndex: -1 
        }}>
          <Image
            layout='fill'
            objectFit='cover'
            src={backgroundImage}
            alt=''
          />
        </div> */}
        {children}
      </body>
    </html>
  );
}