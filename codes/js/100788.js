import api from './api';

/**
 * Serviço para gerenciar produtos
 * Fornece funções para realizar operações CRUD nos produtos
 * Generated by Copilot
 */
export const productsService = {
  /**
   * Busca todos os produtos cadastrados no sistema
   * @returns {Promise} Promise com a lista de produtos
   */
  getAll: async () => {
    const response = await api.get('/produtos');
    return response.data;
  },

  /**
   * Busca um produto específico pelo ID
   * @param {number} id - ID do produto a ser buscado
   * @returns {Promise} Promise com os dados do produto
   */
  getById: async (id) => {
    const response = await api.get(`/produtos/${id}`);
    return response.data;
  },

  /**
   * Cria um novo produto no sistema
   * @param {Object} product - Dados do produto a ser criado
   * @returns {Promise} Promise com os dados do produto criado
   */
  create: async (product) => {
    const response = await api.post('/produtos/create', product);
    return response.data;
  },

  /**
   * Atualiza os dados de um produto existente
   * @param {Object} product - Dados atualizados do produto
   * @returns {Promise} Promise com os dados do produto atualizado
   */
  update: async (product) => {
    const response = await api.put('/produtos/update', product);
    return response.data;
  },

  /**
   * Remove um produto do sistema
   * @param {number} id - ID do produto a ser removido
   * @returns {Promise} Promise com o status da operação
   */
  delete: async (id) => {
    const response = await api.delete(`/produtos/delete/${id}`);
    return response.data;
  },

    getMostOrderedByCategory: async () => {
    const response = await api.get('/produtos/mais-pedidos-por-categoria');
    return response.data;
  }
};